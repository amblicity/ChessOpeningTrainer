import { View } from 'react-native';

export const HelpFullScreenView = openingName => {
  return <View style={styles.container} />;
};

const styles = {
  container: {
    width: '100%',
    height: '100%',
    backgroundColor: 'white',
  },
};
